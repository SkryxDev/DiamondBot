const Discord = require("discord.js")
const db = require("../../models/ticket.model")

module.exports = {
    name: "close",
    description: "[STAFF] Chiudere un ticket",
    onlyStaff: true,
    async execute(interaction) {
        
    }
}